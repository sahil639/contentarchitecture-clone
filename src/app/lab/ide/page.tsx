import { IdeSection } from "@/components/sections/ide-section";

/* Isolation harness, so the section can be checked without scrolling the page. */
export default function IdeLabPage() {
  return <IdeSection id="the-repo" />;
}
