import { TextTerminalSection } from "@/components/sections/text-terminal-section";

/* Isolation harness, so the section can be checked without scrolling past the hero. */
export default function SectionTwoLabPage() {
  return <TextTerminalSection id="text-terminal" />;
}
