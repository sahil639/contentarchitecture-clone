import { CalloutSection } from "@/components/sections/callout-section";

/* Isolation harness, so the section can be checked without scrolling the page. */
export default function CalloutLabPage() {
  return <CalloutSection id="callout" />;
}
